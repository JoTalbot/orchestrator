{
 "POST /api/chat/trigger-token": "РАБОТАЕТ (17.09.2026): тело {\"sessionId\": \"<chat_id>\"} → {\"token\": \"<publicAccessToken JWT>\"}. Дешёвая замена чтению RSC-пейлоада.",
 "POST /api/chat/trigger-session": "Старт/пересоздание Trigger.dev-сессии: {\"sessionId\", \"timezone\"}. GET отдаёт 403 — только POST.",
 "GET /api/chat/agent-models": "403 «Not allowed» даже из вкладки — вероятно нужен контекст страницы агента/права.",
 "GET /api/history/unified": "РАБОТАЕТ: список чатов, query cursor/limit(≤50)/includeArchived/type=agentic → {entries[], nextCursor, total}.",
 "GET /api/history/search": "РАБОТАЕТ: поиск по чатам, query q/limit/includeArchived.",
 "PATCH /api/history/agentic/{id}": "РАБОТАЕТ: {\"title\": \"...\"} → {type,id,title}. Заголовок с переводами строк/управляющими символами → 400.",
 "PATCH /api/history/text/{id}": "То же для обычных (не агентских) чатов.",
 "GET /api/chat/{id}/messages": "РАБОТАЕТ: query cursor,limit(≤50) → {messages[], pagination{cursor,hasMore}} — пагинация НАЗАД по истории.",
 "GET /api/chat/{id}/workspace/latest": "РАБОТАЕТ: ?includeManifest=true → {manifestNodeId, leafMessageId, ...} — снимок песочницы агента.",
 "GET /api/chat/{id}/workspace/{manifestNodeId}": "Содержимое файла/каталога песочницы по nodeId; каталог — суффикс /dir?path=.",
 "GET /api/chat/{id}/preview": "РАБОТАЕТ: состояние превью-песочницы {registered, sandboxState, processes[], ports[]}.",
 "GET /api/chat/{id}/cost": "403 Unauthorized — стоимость недоступна этому аккаунту/маршруту.",
 "POST /api/chat": "НЕ ИСПОЛЬЗОВАТЬ в агент-режиме: 403 «Route not allowed». Сообщения идут в поток Trigger.dev.",
 "POST /api/storage/generate-agent-upload-url": "РАБОТАЕТ: {\"hash\": base64url(sha256(байты)) без '=' (43 символа), \"contentType\", \"size\"} → {uploadUrl, key}; затем PUT uploadUrl с байтами; url файла в сообщении = /api/chat/workspace/cas/user/{hash}.",
 "GET /api/chat/workspace/cas/user/{param}": "Отдача загруженного пользователем файла по CAS-хешу (43 символа).",
 "POST /nextjs-api/stream/create-chat": "РАБОТАЕТ: создание чата Agent Mode. Тело {message:{id:uuid7,role:'user',parts:[{type:'text',text}|{type:'file',url,mediaType,filename}]}, recaptchaV3Token, timezone, modelId?, harnessId?, enabledConnectors?} → {id}.",
 "GET /api/billing/balance": "РАБОТАЕТ: {creditsRemaining, dailyFreeCredits, refreshedAt} — ежедневная квота 1 000 000 кредитов.",
 "GET /api/me/pulse": "РАБОТАЕТ: {pulse} — признак активности/квоты.",
 "GET /api/me": "РАБОТАЕТ: профиль (email, username, subscription, tier).",
 "GET /api/coding/github/connection": "РАБОТАЕТ: {status:'connected'} — GitHub подключён к аккаунту.",
 "GET /api/coding/github/repos": "РАБОТАЕТ: список репозиториев GitHub, подключённых к аккаунту.",
 "GET /api/connectors": "502 — апстрим Cloudflare сломан (маршрут есть, но не работает).",
 "POST /ai-proxy/realtime/v1/sessions/{id}/in/append": "РАБОТАЕТ: «вход» агента. Заголовки Authorization: Bearer <publicAccessToken>, x-trigger-source: sdk, x-part-id: uuid. Тело {kind:'message', payload:{message, chatId, trigger:'submit-message', messageId, metadata}} или {kind:'stop'}.",
 "GET /ai-proxy/realtime/v1/sessions/{id}/out": "РАБОТАЕТ: SSE-поток ответа агента, Authorization: Bearer <publicAccessToken>. Поддерживает Last-Event-ID (без него — реплей всей истории записей) и X-Peek-Settled. События приходят пачками {records:[{seq_num,timestamp,body:'{\"data\":{\"type\":\"text-delta\"|\"reasoning-delta\"|\"tool-input-delta\"|\"start\"...}}'}]}.",
 "GET /ai-proxy/realtime/v1/sessions/{id}/channels/{channel}": "Поток отдельного канала сессии (вариант /out).",
 "GET /ai-proxy/realtime/v1/runs": "Список запусков Trigger.dev (внутренняя отладка).",
 "GET /ai-proxy/realtime/v1/runs/{runId}": "Состояние конкретного запуска.",
 "GET /ai-proxy/realtime/v1/batches/{batchId}": "Состояние батча запусков.",
 "GET /ai-proxy/realtime/v1/streams/{runId}/{streamId}": "Поток запуска.",
 "POST /ai-proxy/realtime/v1/streams/{runId}/{streamId}/append": "Дозапись в поток запуска.",
 "GET /agent/{id}": "HTML страницы чата: внутри RSC-пейлоад (self.__next_f.push) с транскриптом, session.publicAccessToken и курсором пагинации."
}